# Neon / Postgres (preferred persistence)
DATABASE_URL=

# Optional for HubSpot sync from /api/lead
HUBSPOT_ACCESS_TOKEN=

# Optional custom property internal names (defaults shown)
HUBSPOT_PROP_POPIA_SCORE=popia_compliance_score
HUBSPOT_PROP_POPIA_RISK=popia_risk_level
HUBSPOT_PROP_POPIA_GRADE=popia_assessment_grade
HUBSPOT_PROP_POPIA_DATE=popia_assessment_date

# Optional for Resend email delivery from /api/lead
RESEND_API_KEY=
EMAIL_FROM=
EMAIL_REPLY_TO=

# Optional guard for internal operational APIs
# Used by /api/leads and /api/lead/:leadId as x-internal-token or Bearer token
INTERNAL_API_TOKEN=

# Optional report-link signing secret. If set, /api/report/:leadId requires a valid token.
REPORT_SIGNING_SECRET=
